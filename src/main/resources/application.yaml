server:
  port: 8081

spring:
  application:
    name: agendador-tarefas
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration

  data:
    mongodb:
<<<<<<< HEAD
      host: localhost
      port: 27017

jwt:
  secret: ${JWT_SECRET:minhaChaveSuperSecretaParaAssinar0Token1234567890}
  expiration: 3600000
=======
      # URI unificada que o MongoConfig e os testes esperam encontrar
      uri: ${MONGODB_URI:mongodb://localhost:27017/db_agendador}
>>>>>>> origin/develop

usuario:
  service:
    url: ${USUARIO_SERVICE_URL:http://localhost:8080}

tarefa:
  max-tentativas: 5

jwt:
  secret: ${APP_JWT_SECRET:chave-super-secreta-minima-32-caracteres-para-HS512-2026}
  expiration-ms: 3600000
  service-expiration-ms: 1800000